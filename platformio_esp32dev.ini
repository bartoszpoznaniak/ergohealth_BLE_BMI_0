[env:esp32cam]
platform = ${env:esp32dev.platform}
board = esp32cam
framework = ${env:esp32dev.framework}
monitor_speed = ${env:esp32dev.monitor_speed}
monitor_rts = ${env:esp32dev.monitor_rts}
monitor_dtr = ${env:esp32dev.monitor_dtr}

lib_deps =
    ${env:esp32dev.lib_deps}